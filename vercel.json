{
  "version": 2,
  "builds": [
    {
      "src": "**/*.html",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    { "src": "/", "dest": "/api/header" },
    { "src": "/about", "dest": "/api/header?page=about" },
    { "src": "/requests", "dest": "/api/header?page=requests" }
  ]
}
